<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/info" method="get">
        <label for="id">이름
        <input type="text" name="id" placeholder="이름을 입력하세요" id="id" minlength="2"> </label> <br>
        <fieldset>
            <legend>성별</legend>
            <input type="radio" name="who" value="남성">남성
            <input type="radio" name="who" value="여성">여성

        </fieldset>

        <fieldset>
        <legend>생년월일</legend>
        <select name="birthYear" id="year">
          <% for(let i = 0; i < 50; i++) { %>
          <option value="<%= 2010 - i %>"><%= 2010 - i  %></option>
          <% } %>
        </select>년
        <select name="birthMonth" id="Month">
            <% for(let i = 0; i < 12; i++) { %>
            <option value="<%= 1 + i %>"><%= 1 + i  %></option>
            <% } %>
        </select>월
        <select name="birthDay" id="Day">
            <% for(let i = 0; i < 31; i++) { %>
            <option value="<%= 1 + i %>"><%= 1 + i  %></option>
            <% } %>
        </select>일
    </fieldset>
    <fieldset>
        <legend>관심사</legend>
        <input type="checkbox" name="fav" value="여행">여행
        <input type="checkbox" name="fav" value="패션">패션
        <input type="checkbox" name="fav" value="음식">음식
    </fieldset>
    <input type="submit" value="회원가입">
    </form>
</body>
</html>